# ✅ Финальная инструкция - Деплой на 404tears.kz

## 🚀 Запуск одной командой:

```bash
./start.sh
```

**Всё!** Сайт запущен и работает.

## 📋 Что уже настроено:

✅ `.env.example` - файл с настройками по умолчанию  
✅ `docker-compose.prod.yml` - конфигурация для продакшена  
✅ `start.sh` - скрипт автоматического запуска  
✅ `init-ssl.sh` - скрипт настройки SSL  
✅ Nginx конфигурации для HTTP и HTTPS  
✅ Автоматическое добавление товаров  

## 🔧 Что нужно сделать перед запуском:

### 1. Настроить DNS
Убедитесь, что DNS записи настроены:
- `404tears.kz` → IP вашего сервера
- `www.404tears.kz` → IP вашего сервера

### 2. Запустить проект
```bash
# Скрипт автоматически создаст .env из .env.example
./start.sh
```

При первом запуске скрипт:
- Создаст `.env` файл
- Попросит отредактировать пароль
- После редактирования запустите `./start.sh` снова

### 3. Настроить SSL (HTTPS)
```bash
./init-ssl.sh
```

## 📁 Структура .env файла:

```env
POSTGRES_USER=shop_user
POSTGRES_PASSWORD=change_this_strong_password_123!  # ИЗМЕНИТЕ!
POSTGRES_DB=shop

DOMAIN=404tears.kz
DOMAIN_WWW=www.404tears.kz
LETSENCRYPT_EMAIL=admin@404tears.kz

ALLOWED_ORIGINS=https://404tears.kz,https://www.404tears.kz
```

## 🌐 Проверка работы:

После запуска `./start.sh`:

- **HTTP:** http://404tears.kz
- **API:** http://404tears.kz/api/products
- **Admin:** http://404tears.kz/api/admin/add-products

После `./init-ssl.sh`:

- **HTTPS:** https://404tears.kz

## 📝 Полезные команды:

```bash
# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f

# Остановка
docker-compose -f docker-compose.prod.yml down

# Перезапуск
docker-compose -f docker-compose.prod.yml restart

# Обновление после изменений кода
git pull
docker-compose -f docker-compose.prod.yml up -d --build
```

## ⚠️ Важно:

1. **Измените пароль** в `.env` перед первым запуском
2. **Настройте DNS** до запуска `./init-ssl.sh`
3. **Откройте порты 80 и 443** в firewall сервера

## 🔍 Если что-то не работает:

1. Проверьте логи: `docker-compose -f docker-compose.prod.yml logs`
2. Проверьте статус: `docker-compose -f docker-compose.prod.yml ps`
3. Убедитесь, что порты открыты: `sudo ufw allow 80 && sudo ufw allow 443`
4. Проверьте DNS: `nslookup 404tears.kz`
