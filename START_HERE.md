# 🚀 Быстрый старт - Деплой на 404tears.kz

## Одна команда для запуска!

```bash
./start.sh
```

Всё! Сайт запущен и работает.

## Что нужно сделать:

### 1. Настроить DNS (один раз)
Убедитесь, что DNS записи настроены:
- `404tears.kz` → IP вашего сервера  
- `www.404tears.kz` → IP вашего сервера

### 2. Запустить проект
```bash
# Скопировать .env (если еще не скопирован)
cp .env.example .env

# Отредактировать пароль (ВАЖНО!)
nano .env
# Измените POSTGRES_PASSWORD на сильный пароль!

# Запустить
./start.sh
```

### 3. Настроить SSL (HTTPS) - опционально
```bash
./init-ssl.sh
```

## Что делает start.sh:

✅ Проверяет и создает .env файл  
✅ Выбирает правильную конфигурацию (HTTP/HTTPS)  
✅ Собирает и запускает все Docker контейнеры  
✅ Добавляет тестовые товары автоматически  
✅ Показывает статус и ссылки  

## Структура .env файла:

```env
POSTGRES_USER=shop_user
POSTGRES_PASSWORD=ваш_сильный_пароль  # ИЗМЕНИТЕ!
POSTGRES_DB=shop

DOMAIN=404tears.kz
DOMAIN_WWW=www.404tears.kz
LETSENCRYPT_EMAIL=admin@404tears.kz

ALLOWED_ORIGINS=https://404tears.kz,https://www.404tears.kz
```

## Проверка работы:

- **HTTP:** http://404tears.kz
- **HTTPS:** https://404tears.kz (после `./init-ssl.sh`)
- **API:** http://404tears.kz/api/products
- **Admin:** http://404tears.kz/api/admin/add-products

## Полезные команды:

```bash
# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f

# Остановка
docker-compose -f docker-compose.prod.yml down

# Перезапуск
docker-compose -f docker-compose.prod.yml restart

# Обновление кода
git pull
docker-compose -f docker-compose.prod.yml up -d --build
```

## Если что-то не работает:

1. Проверьте логи: `docker-compose -f docker-compose.prod.yml logs`
2. Проверьте статус: `docker-compose -f docker-compose.prod.yml ps`
3. Убедитесь, что порты 80 и 443 открыты в firewall
4. Проверьте DNS настройки
