#!/bin/bash

# Скрипт для исправления nginx конфигурации на сервере

echo "🔧 Исправление nginx конфигурации..."

# Используем HTTP конфигурацию (без SSL)
cp nginx/nginx-http.conf nginx/nginx.conf

echo "✅ Конфигурация обновлена на HTTP"

# Перезапускаем frontend
echo "🔄 Перезапуск frontend..."
docker-compose -f docker-compose.prod.yml restart frontend

echo "✅ Готово! Frontend должен работать на HTTP"
echo "Теперь можно получить SSL сертификат через ./init-ssl.sh"
